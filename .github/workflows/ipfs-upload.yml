
name: Upload to IPFS

on:
  push:
    branches: [main]

jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Upload file to IPFS
        uses: aquiladev/ipfs-action@main
        with:
          path: jinnz2-archive-1.zip
